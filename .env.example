# 和风天气API配置
# 请在 https://dev.qweather.com/ 注册账号并获取免费API密钥
# 然后创建 .env.local 文件并添加以下配置：
REACT_APP_QWEATHER_API_KEY=your_api_key_here

# 可选：自定义和风天气API的host地址
# 如果不配置，默认使用 https://devapi.qweather.com/v7
# REACT_APP_QWEATHER_HOST=https://your-custom-host.com/v7

# 注意事项：
# 1. 免费版API每天有1000次调用限制
# 2. 可以通过REACT_APP_QWEATHER_HOST环境变量自定义API host
# 3. 如果不配置API密钥，应用会使用模拟天气数据
# 4. 自定义host可以提供更稳定的API访问服务